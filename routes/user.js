const express = require('express');
const router  = express.Router();

// /* GET home page */
// router.get('/login', (req, res, next) => {
//   let img = {icon: "img/cb-icon.svg"}
//   res.render('login', {content: img});
// });

module.exports = router;